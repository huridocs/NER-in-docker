services:
  ner-gpu:
    extends:
      file: docker-compose.yml
      service: ner
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]

  pdf-layout-analysis-gpu:
    extends:
      file: docker-compose.yml
      service: pdf-layout-analysis
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
  
  ner-ui:
    extends:
      file: docker-compose.yml
      service: ner-ui

  ollama:
    extends:
      file: docker-compose.yml
      service: ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]

  postgres:
    extends:
      file: docker-compose.yml
      service: postgres

  adminer:
    extends:
      file: docker-compose.yml
      service: adminer

networks:
  network-ner:
    driver: bridge

volumes:
  postgres_data: